# Minimal requirements for CI/CD pipeline - faster installation
# Essential packages only

# Core ML packages
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# MLflow and tracking (essential for model ops)
mlflow>=2.8.0
dagshub>=0.3.0

# Flask for web service
flask>=2.3.0

# DVC for data versioning
dvc[s3]>=3.0.0

# NLTK for text processing
nltk>=3.8

# AWS integration
boto3>=1.28.0

# Utilities
tqdm>=4.65.0
joblib>=1.3.0
python-dotenv>=0.5.1

# Testing (minimal)
pytest>=7.4.0

# Production server
gunicorn>=21.0.0