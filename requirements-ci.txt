# SUPER MINIMAL CI requirements - FASTEST installation possible
# Only absolute essentials for CI testing

# Core essentials (pre-compiled wheels available)
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<1.4.0

# Flask (lightweight)
flask>=2.3.0,<3.0.0

# NLTK (only for text processing)
nltk>=3.8,<4.0.0

# MLflow (minimal - no UI dependencies)
mlflow-skinny>=2.8.0,<3.0.0

# Testing only
pytest>=7.4.0,<8.0.0

# Basic utilities
joblib>=1.3.0,<2.0.0
python-dotenv>=0.5.1,<2.0.0

# AWS (minimal)
boto3>=1.28.0,<2.0.0